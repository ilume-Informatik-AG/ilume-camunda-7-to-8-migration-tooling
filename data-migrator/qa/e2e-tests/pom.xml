<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.camunda</groupId>
    <artifactId>camunda-7-to-8-data-migrator-qa</artifactId>
    <version>0.3.6-SNAPSHOT</version>
  </parent>

  <artifactId>camunda-7-to-8-data-migrator-e2e-tests</artifactId>
  <packaging>pom</packaging>

  <name>Camunda 7 to 8 Migration Tooling - Data - E2E Tests</name>
  <description>End-to-end tests for the Cockpit plugin using Playwright</description>


  <dependencies>
    <dependency>
      <groupId>io.camunda</groupId>
      <artifactId>camunda-7-to-8-data-migrator</artifactId>
      <version>${project.version}</version>
      <type>zip</type>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>docker-compose.yml.template</include>
        </includes>
      </resource>
    </resources>

    <plugins>
      <!-- Filter docker-compose.yml.template and rename to docker-compose.yml -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <executions>
          <execution>
            <id>filter-docker-compose</id>
            <phase>process-resources</phase>
            <goals>
              <goal>resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}</outputDirectory>
              <resources>
                <resource>
                  <directory>src/main/resources</directory>
                  <filtering>true</filtering>
                  <includes>
                    <include>docker-compose.yml.template</include>
                  </includes>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Rename docker-compose.yml.template to docker-compose.yml -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.2.0</version>
        <executions>
          <execution>
            <id>rename-docker-compose</id>
            <phase>process-resources</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <move file="${project.basedir}/docker-compose.yml.template"
                      tofile="${project.basedir}/docker-compose.yml"
                      overwrite="true"/>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Frontend Maven Plugin for npm/Node.js integration -->
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <executions>
          <!-- Install Node.js and npm -->
          <execution>
            <id>install-node-and-npm</id>
            <phase>initialize</phase>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
          </execution>

          <!-- Install npm dependencies -->
          <execution>
            <id>npm-install</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>ci</arguments>
            </configuration>
          </execution>

          <!-- Install Playwright browsers -->
          <execution>
            <id>install-playwright-browsers</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>npx</goal>
            </goals>
            <configuration>
              <arguments>playwright install --with-deps chromium firefox msedge</arguments>
            </configuration>
          </execution>

          <!-- Run E2E tests -->
          <execution>
            <id>run-e2e-tests</id>
            <phase>integration-test</phase>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>test</arguments>
              <environmentVariables>
                <CI>true</CI>
              </environmentVariables>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>

